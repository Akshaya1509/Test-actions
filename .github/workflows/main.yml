name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Run a one-line script
      run: 
        Get-Module -Name Az.Accounts -ListAvailable
      shell:
        pwsh
