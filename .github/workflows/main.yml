name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Azure CLI Action
      with:
        # Specify the script here
        inlineScript: 
          echo "foo"
    
